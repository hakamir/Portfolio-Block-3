FRONTEND_URL=                      # Frontend URL
FRONTEND_PORT=                     # Frontend Port

JWT_SECRET_KEY=                    # JWT key
JWT_ACCESS_TOKEN_EXPIRES=          # In minutes
JWT_REFRESH_TOKEN_EXPIRES=         # In days
JWT_COOKIE_SECURE=                 # True in prod, false in dev
JWT_COOKIE_SAMESITE=Strict
JWT_COOKIE_CSRF_PROTECT=           # True in prod, false in dev

MONGO_ROOT_USER=                # Your admin user
MONGODB_ROOT_PASSWORD=            # Your admin password
MONGODB_USER=                      # The user that will be create with access to the DB
MONGODB_PASSWORD=                  # The user password
MONGODB_HOST=
MONGODB_PORT=27017
MONGODB_DATABASE=Portfolio
MONGODB_TIMEOUT=500

TEST_USER_EMAIL=                   # Will be the email used to access the admin part of the portfolio
TEST_USER_PASSWORD=                # The password of the user