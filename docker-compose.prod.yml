# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:

  frontend:
    image: ghcr.io/${GHCR_OWNER}/frontend:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt/live/deviance.studio:/etc/letsencrypt/live/deviance.studio:ro
      - /etc/letsencrypt/archive/deviance.studio:/etc/letsencrypt/archive/deviance.studio:ro

  backend:
    image: ghcr.io/${GHCR_OWNER}/backend:latest
    environment:
      FLASK_ENV: production
      FRONTEND_URL: https://deviance.studio
      FRONTEND_PORT: 443
      JWT_COOKIE_SECURE: "true"
      JWT_COOKIE_CSRF_PROTECT: "true"

  seeder:
    image: ghcr.io/${GHCR_OWNER}/backend:latest